import ComingSoon from "@/components/ComingSoon";

export const revalidate = 86400;

export const metadata = { title: "이용약관 — 널스넷", robots: { index: false } };

export default function TermsPage() {
  return <ComingSoon title="이용약관" />;
}
