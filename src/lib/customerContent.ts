/**
 * 이벤트 — 구 널스넷에서 옮겼다(2026-07-30, 레거시 폐기 전 확보).
 *
 * 🔴 공지사항·FAQ 는 2026-08-04 에 DB(site_posts)로 옮겼다 — 관리자 화면(/admin/content)에서
 *    직접 쓴다. 이벤트만 파일에 남은 이유는 **본문이 포스터 이미지 한 장**이라서다.
 *    글자만 담는 표로는 옮길 수 없고, 이미지 업로드까지 붙이려면 스토리지·용량·변환이 따라온다.
 *    이벤트를 화면에서 등록해야 할 일이 생기면 그때 이미지 처리와 함께 옮긴다.
 */

export type EventPost = {
  id: string;
  title: string;
  date: string;
  /** 레거시 이벤트 글은 본문이 포스터 이미지 한 장뿐이었다 */
  image: string;
  width: number;
  height: number;
  /** 이미지에 적힌 문구 — 이미지를 못 보는 사람과 검색엔진을 위해 옮겨 적는다 */
  alt: string;
  /** 종료일이 지난 이벤트는 '종료' 로 표시한다 */
  endedAt: string;
};

/**
 * 🔴 레거시 이벤트 2건 중 **1건만** 옮겼다.
 *    빠진 것은 제목이 'event' 인 2024-10-15 글로, 본문 이미지가 "내돈내산 찐리뷰 SNS공유 이벤트"
 *    (재구매·특급배송·투데이쇼핑 문구가 든 쇼핑몰용 스톡 배너)였다. 널스넷과 무관한
 *    게시판 스킨 샘플이라 간호사 채용 사이트에 그대로 올리면 잘못된 안내가 된다.
 *    이미지는 public/img/event/18107.png 에 받아 뒀으니, 오너가 넣으라고 하면 한 줄이면 된다.
 */
export const EVENTS: readonly EventPost[] = [
  {
    id: "starbucks",
    title: "스타벅스 커피 이벤트",
    date: "2024-10-19",
    endedAt: "2024-12-31",
    image: "/img/event/19467.png",
    // 파일에서 읽은 실제 치수다 — 어림잡아 적으면 next/image 가 그 비율로 자리를 잡아 그림이 눌린다
    width: 1626,
    height: 1080,
    alt: "널스넷 × 스타벅스 — 스타벅스 커피를 첫 가입 간호사라면 누구나! 선착순 1000명",
  },
];
