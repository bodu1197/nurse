/**
 * 🗂 공고 검색 축 — 어휘와 분류 규칙을 여기 한 곳에 둔다.
 *
 * 왜 이 파일이 생겼나(2026-07-29 실측):
 *   진료과 필터가 손으로 적은 9개(중환자실·응급실·수술실·병동·외래·요양병원·정신과·마취과·투석실)였다.
 *   그 결과 —
 *     · 자체 광고 44건 중 **32건(73%)** 이 진료과 값은 멀쩡한데 칩이 없어 검색되지 않았다
 *       (내과 5, 안과 3, 정형외과 3, 산부인과 3 … 전부 이력서 쪽 표에는 있는 값이다).
 *     · 워크넷 공고는 47%가 '요양병원' 한 칩에 몰리고 41.5%가 미분류였다.
 *     · '병동·외래·요양병원' 은 진료과가 아니라 근무지 유형이라 축이 섞여 있었다.
 *
 * 그래서 축을 셋으로 가른다. 어휘는 **이력서(resumeOptions)와 공유**한다 —
 * 인재와 공고가 다른 말을 쓰면 "내과 간호사"를 찾는 병원이 내과 인재를 못 만난다.
 *
 *   1) 진료과   specialty      = DEPARTMENTS(28) …… 구 널스넷 근무부서 코드표
 *   2) 기관 종별 facility_type  = FACILITY_TYPES(11) … 이력서 HOSPITAL_TYPES + 워크넷 실측 보강
 *   3) 직종     job_category   = JOB_CATEGORIES(8) … 구 널스넷 직종 대분류
 */

import { DEPARTMENTS, JOB_CATEGORIES, HOSPITAL_TYPES } from "./resumeOptions.ts";

export { DEPARTMENTS as JOB_DEPARTMENTS, JOB_CATEGORIES };

/**
 * 🏥 기관 종별 — 이력서의 HOSPITAL_TYPES 를 바탕으로 하되 워크넷 실측에 맞춰 둘을 더했다.
 *
 * '요양원·주간보호' 는 의료기관이 아니라 장기요양기관이라 HOSPITAL_TYPES 에 없었는데,
 * 워크넷 간호 공고의 **47%** 가 여기다(노인요양복지시설 37.3% + 주간보호·방문·재가 등).
 * 이걸 빼면 게시판 절반을 거를 방법이 없어진다. '치과' 도 같은 이유(1.6%, 이력서 표엔 없음).
 *
 * '산후조리원' 도 따로 뒀다 — 산업분류가 제각각이라 요양원·기타·보건소로 흩어져 있었고,
 * 그래서 요양원 칩 아래에 신생아실·분만실이 뜨는 기이한 화면이 나왔다(오너 지적 2026-07-29).
 * 노인 돌봄과 신생아 돌봄은 간호사에게 완전히 다른 자리다.
 *
 * 순서 = 화면 노출 순서가 아니다(칩은 공고 수 순으로 그린다). 규모가 큰 기관부터 적어 읽기 좋게 둔다.
 */
export const FACILITY_TYPES = [
  "상급종합병원", "종합병원", "병원", "요양병원", "한방병원", "치과",
  "의원", "검진센터", "보건소", "산후조리원", "요양원·주간보호", "기타",
] as const;
export type FacilityType = (typeof FACILITY_TYPES)[number];

/**
 * 이력서 쪽 표(HOSPITAL_TYPES)의 항목이 전부 여기 있어야 인재↔공고가 서로 잡힌다.
 * `readonly string[]` 로 받으면 원소가 하나도 검사되지 않으므로(둘 다 string[] 이라 무조건 통과),
 * 리터럴 유니온끼리 비교해 **빠진 이름이 있으면 컴파일이 실패**하게 한다.
 * 지금 FACILITY_TYPES 에 없는 것: '전문병원' — 워크넷 산업분류에 대응 값이 없어 뺐다.
 */
type MissingFromFacility = Exclude<(typeof HOSPITAL_TYPES)[number], FacilityType | "전문병원">;
const _facilityCoversHospitalTypes: MissingFromFacility[] = [];
void _facilityCoversHospitalTypes;

/**
 * 워크넷 산업분류(indTpNm) → 기관 종별.
 *
 * 워크넷은 진료과를 주지 않는다. 표준산업분류(KSIC)를 쓰는 전 산업 구인 사이트라 병원 내부
 * 진료과라는 개념이 스키마에 없다. 대신 **산업분류를 100% 준다**(실측 1,457건 중 누락 0건).
 * 이 표로 96.8% 가 분류된다.
 *
 * 🔴 순서가 규칙이다. 위에서부터 첫 매칭. '요양 병원' 을 '병원' 보다 먼저 봐야 하고,
 *    '치과 의원' 을 '의원' 보다 먼저 봐야 한다. 순서를 바꾸면 조용히 오분류된다.
 */
const FACILITY_RULES: ReadonlyArray<readonly [FacilityType, RegExp]> = [
  ["요양병원", /요양\s*병원/],
  // 노인요양복지시설 37.3% · 주간보호 · 방문복지 · 재가 · 거주복지시설 — 합쳐 워크넷의 47%
  ["요양원·주간보호", /노인\s*요양\s*복지시설|노인\s*양로|노인\s*거주\s*복지|주간\s*보호|비거주\s*복지|거주\s*복지시설|방문\s*복지|사회복지\s*상담|개인\s*간병|재가|직업재활원|신체\s*부자유자/],
  ["상급종합병원", /상급\s*종합/],
  ["종합병원", /종합\s*병원/],
  ["치과", /치과/],
  ["한방병원", /한방\s*병원|한의원|한방/],
  // 공중보건의료업(보건소·보건지소) + 보건복지 행정. 지자체 소속 간호 공고가 여기 모인다.
  ["보건소", /공중\s*보건\s*의료|보건\s*및\s*복지\s*행정|사회보장\s*행정|기타\s*사회서비스\s*관리\s*행정/],
  ["검진센터", /방사선\s*진단|병리\s*검사|건강\s*진단|검진/],
  ["병원", /병원/],
  ["의원", /의원|피부\s*미용/],
  // '그 외 기타 보건업' 15.2% 가 여기로 온다 — 보건업인 건 맞는데 종별을 알 수 없는 값이다.
  ["기타", /보건업|의료/],
];

/** 워크넷 산업분류명 → 기관 종별. 못 가리면 null(= 기관 종별 필터에서 "전체"로만 노출). */
export function facilityFromIndustry(indTpNm: string | null | undefined): FacilityType | null {
  const s = (indTpNm ?? "").trim();
  if (!s) return null;
  return FACILITY_RULES.find(([, re]) => re.test(s))?.[0] ?? null;
}

/**
 * 🔴 회사명이 산업분류보다 정확한 경우 — 여기 걸리면 산업분류를 무시한다.
 *
 * 산후조리원 18건이 산업분류만 보면 요양원(8)·기타(2)·보건소(1)·병원(1)·미분류(6)로 흩어졌다.
 * 업체가 사업자등록을 어떻게 냈느냐에 따라 KSIC 가 제각각이기 때문이다. 이름에 "산후조리" 가
 * 들어가면 그건 산후조리원이 맞다 — 애매할 여지가 없는 이름만 여기 둔다.
 */
const FACILITY_BY_NAME: ReadonlyArray<readonly [FacilityType, RegExp]> = [
  ["산후조리원", /산후\s*조리/],
];

/**
 * 🏥 심사평가원 종별(clCdNm) → 우리 기관 종별. **이게 정답 소스다** — 추정이 아니라 복지부 지정값이다.
 *
 * 산업분류(KSIC)로는 '상급종합' 을 절대 못 가려낸다(그 개념이 KSIC 에 없다). 그래서 FACILITY_TYPES 에
 * '상급종합병원' 칸이 있는데도 0건이었고 칩이 화면에 안 그려졌다. 명부에 clCdNm 을 저장하면서 열린다.
 *
 * 🔴 순서가 규칙이다. '치과병원'·'한방병원'·'요양병원' 을 '병원' 보다 **먼저** 봐야 한다 —
 *    아래로 내리면 전부 '병원' 으로 뭉개진다.
 * 🔴 '정신병원' 은 우리 표에 칸이 없어 '병원' 으로 둔다. 새 칸을 만들면 이력서 쪽 HOSPITAL_TYPES 와
 *    어긋나 인재↔공고 매칭이 깨진다(축 어휘를 공유하는 것이 이 파일의 설계다). 정신과 자리는
 *    진료과(specialty) 축의 '정신과' 로 잡힌다.
 * 🔴 표에 못 넣는 값은 **'기타' 가 아니라 null 이다.** '기타' 를 주면 부르는 쪽에서
 *    `명부값 ?? 산업분류값` 순으로 고를 때 **덜 정확한 '기타' 가 더 정확한 산업분류 유도값을 이긴다**
 *    (sync-worknet). null 이면 다음 후보로 자연히 내려간다.
 */
const FACILITY_BY_CL_CD: ReadonlyArray<readonly [FacilityType, RegExp]> = [
  ["상급종합병원", /상급\s*종합/],
  ["종합병원", /종합\s*병원/],
  ["치과", /치과/],
  ["한방병원", /한방\s*병원|한의원|한방/],
  ["요양병원", /요양\s*병원/],
  ["보건소", /보건소|보건지소|보건진료소|보건의료원/],
  ["병원", /병원/], // 정신병원 포함(위에서 안 걸린 '○○병원' 전부)
  ["의원", /의원/],
];

export function facilityFromClCd(clCdNm: string | null | undefined): FacilityType | null {
  const s = (clCdNm ?? "").trim();
  if (!s) return null;
  return FACILITY_BY_CL_CD.find(([, re]) => re.test(s))?.[0] ?? null;
}

/** 회사명 + 산업분류로 기관 종별을 정한다. 이름이 확실하면 이름을, 아니면 산업분류를 쓴다. */
export function facilityFor(
  companyName: string | null | undefined,
  indTpNm: string | null | undefined,
): FacilityType | null {
  const name = (companyName ?? "").trim();
  const byName = name ? FACILITY_BY_NAME.find(([, re]) => re.test(name))?.[0] : undefined;
  return byName ?? facilityFromIndustry(indTpNm);
}

/**
 * 워크넷 직종코드(jobsCd) → 직종.
 *
 * 워크넷이 코드로 **정확히** 준다(누락 0건). 제목에서 "간호조무" 를 찾아 추측할 이유가 없다.
 *   304000 간호사 / 304001 전문간호사 / 304002 일반간호사 / 304003 기타간호사(보건교사 등)
 *   307500·307501·307502 간호조무사
 * 이 표에 없는 코드는 간호 직군이 아니다 → 수집 단계에서 버린다(isNurseJobCode).
 */
const CATEGORY_BY_JOB_CODE: Readonly<Record<string, (typeof JOB_CATEGORIES)[number]>> = {
  "304000": "간호직", "304001": "간호직", "304002": "간호직", "304003": "간호직",
  "307500": "간호조무직", "307501": "간호조무직", "307502": "간호조무직",
};

export function categoryFromJobCode(jobsCd: string | null | undefined): (typeof JOB_CATEGORIES)[number] | null {
  return CATEGORY_BY_JOB_CODE[(jobsCd ?? "").trim()] ?? null;
}

/**
 * 이 공고가 간호 직군인가.
 *
 * 🔴 워크넷의 occupation 파라미터는 정확 필터가 아니다(실측): occupation=304000 으로 요청해도
 *    요양보호사(550100) · 치과위생사(306400) 등이 **7.1%** 섞여 온다. 공고 하나에 직종이 여럿
 *    달릴 수 있어(상세의 relJobsNm 에 "간호조무사 물리치료사" 같은 값이 있다) 부수 직종으로도
 *    매칭되고, 응답의 jobsCd 는 대표 직종만 준다. 그래서 응답을 받은 뒤 한 번 더 거른다.
 */
export const isNurseJobCode = (jobsCd: string | null | undefined): boolean =>
  categoryFromJobCode(jobsCd) !== null;

/**
 * 진료과 추론 — 공고 텍스트에서 DEPARTMENTS 어휘를 찾는다.
 *
 * 🔴 순서가 규칙이다. 구체적인 것부터 본다 — '정형외과' 를 '외과' 보다, '혈액종양내과' 를 '내과'
 *    보다 먼저 봐야 한다. 뒤집으면 정형외과 공고가 전부 '외과' 로 떨어진다.
 *
 * 커버리지(상세 400건 실측): 제목만 보면 10.8%, 사업내용·직무내용까지 보면 22.3%.
 * 22% 가 낮아 보이지만 기관 종별로 쪼개면 이유가 분명하다 —
 *   의원 100% · 병원 76% · 종합병원 62% / 요양원 4% · 요양병원 11%.
 * 요양·간병 공고는 진료과 개념이 원래 없다. 그런 공고는 null 로 두는 게 맞다(억지로 채우면
 * 예전처럼 '요양병원' 칩에 47%가 몰려 필터가 무의미해진다).
 */
const DEPARTMENT_RULES: ReadonlyArray<readonly [(typeof DEPARTMENTS)[number], RegExp]> = [
  ["혈액종양내과", /혈액종양|종양내과|항암/],
  ["정신건강의학과", /정신건강|정신과|정신의학|폐쇄병동|정신병원/],
  ["소아청소년과", /소아|청소년과/],
  ["산부인과", /산부인과|여성의학/],
  ["정형외과", /정형외과/],
  ["신경외과", /신경외과/],
  ["흉부외과", /흉부외과/],
  ["성형외과", /성형외과/],
  ["이비인후과", /이비인후/],
  ["비뇨기과", /비뇨/],
  ["신경과", /신경과/],
  ["피부과", /피부과/],
  ["안과", /안과/],
  // "마취통증의학과" 같은 과 이름과 회복실만. 그냥 "마취" 는 자격·업무 설명에도 흔하다.
  ["마취과/회복실", /마취과|마취통증|회복실|마취\s*(?:간호|실|파트|담당)|PACU/i],
  ["인공신장실", /인공신장실|인공신장|투석실|투석\s*(?:간호|파트|담당|업무)/],
  // 🔴 신생아실을 중환자실보다 **먼저** 본다. "NICU"(신생아중환자실)는 ICU 를 품고 있어서,
  //    순서가 뒤면 신생아중환자실 공고가 전부 그냥 중환자실로 떨어진다.
  ["신생아실", /신생아실|신생아\s*(?:간호|케어|파트|담당)|NICU/i],
  // ICU 는 단어로만 — 경계가 없으면 "medicure"·"meticulous" 같은 영단어가 걸린다.
  ["중환자실", /중환자실|중환자\s*(?:간호|파트|담당)|\bICU\b/i],
  ["응급실", /응급실|응급의료|응급센터/],
  // 'OR'(operating room)는 넣지 않는다 — 본문까지 훑으므로 "camc.or.kr" 같은 주소가 전부
  // 수술실로 잡힌다(실측 400건 중 2건). SQL 백필도 같은 이유로 빼 두었다.
  ["수술실", /수술실|스크럽|scrub/i],
  // 🔴 `/분만/` 이면 "오래 하실 **분만** 연락주세요", "경험 있으신 **분만** 지원" 같은
  //    〈분(사람) + 만(조사)〉 을 전부 분만실로 잡는다. 실측: 15건 중 진짜는 3건뿐이었다
  //    (요양원에 분만실이 뜬 게 이것 — 오너 지적 2026-07-29).
  ["분만실", /분만실|분만장|분만센터|분만\s*(?:간호|업무|파트|담당|병동)|\bLDR\b/],
  ["혈관조영실", /혈관조영|심혈관센터|angio/i],
  ["내시경실", /내시경/],
  // 🔴 "직원 **건강검진** 지원", "채용**건강검진** 1개월 이내" 는 복리후생·제출서류지
  //    진료과가 아니다. 실측: 16건 중 진짜 검진센터는 2건뿐이었다.
  //    '센터' 가 붙은 표현만 부서로 본다.
  //    '센터·실' 이 붙거나 검진이 곧 업무인 표현만 잡는다. "특수검진실"·"종합검진 간호사" 같은
  //    진짜를 놓치지 않으면서, 복리후생 문구는 걸리지 않는다(실측 오탐 0).
  ["건강진단센터", /검진센터|건진센터|건강검진센터|종합검진센터|건강증진센터|검진실|건진실|종합검진|건강검진\s*(?:간호|파트|담당|업무)|검진\s*간호/],
  ["주사실", /주사실/],
  ["내과", /내과/],
  ["외과", /외과/],
];

/**
 * 저장된 진료과가 **지금 규칙으로 설명되는가**.
 *
 * 왜 필요한가: 규칙을 고쳐도 이미 저장된 값은 안 바뀐다. 크론이 `?? 저장값` 으로 옛 값을
 * 살려두면 오분류가 영원히 남는다 — 실제로 "요양원에 분만실 8건"을 정리한 직후 크론 한 번에
 * 되살아났다(2026-07-29). 근거가 사라진 값은 버려야 규칙 수정이 데이터에 반영된다.
 *
 * 🔴 **우선순위까지 본다**(자기 규칙만 보면 안 된다). 본문에 "정형외과" 가 있는데 저장값이
 *    '외과' 면, 자기 규칙만으로는 통과해 낮은 우선순위 값이 눌러앉아 재분류를 막는다.
 *    "지금 규칙으로 이 텍스트를 다시 분류해도 같은 값이 나오는가" 를 묻는다.
 */
export function stillValid(specialty: string, text: string): boolean {
  return departmentFromText(text) === specialty;
}

/** 공고 제목·사업내용·직무내용에서 진료과를 고른다. 못 고르면 null. */
export function departmentFromText(...parts: ReadonlyArray<string | null | undefined>): (typeof DEPARTMENTS)[number] | null {
  const text = parts.filter(Boolean).join(" ");
  if (!text.trim()) return null;
  return DEPARTMENT_RULES.find(([, re]) => re.test(text))?.[0] ?? null;
}
