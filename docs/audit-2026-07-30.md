# 널스넷 전 생애주기 감사 · 수정 체크리스트

> 작성 2026-07-30. 간호사·병원 두 페르소나의 전 생애주기를 실제 코드로 추적한 결과 **58건**.
> **완료한 항목은 `~~취소선~~` + `[x]`** 로 표시한다. `[~]` 는 부분 수정.
>
> **진행 (2026-07-30): 53 완료 · 2 부분 · 21 남음.** (7·8·9장 포함)
> 남은 것은 대부분 (a) 새 기능·DB 마이그레이션이 필요하거나 (b) SMTP·도메인 연결을 기다리거나 (c) 오너 결정이 필요한 것이다.

## 오너 확정 사항 (수정 방향을 가르는 전제)

| # | 확정 | 근거 |
|---|---|---|
| A | **결제는 도메인(nursenet.co.kr) 연결 시 이어진다.** 유료 옵션을 감추지 않는다. | 오너 2026-07-30 |
| B | **SEO 문구는 레거시(https://nursenet.co.kr/)를 그대로 쓴다.** 오랜 기간 축적된 메타데이터다. | 오너 2026-07-30 |
| C | **인재정보 비로그인 공개는 레거시와 같은 정책이다** — 게이트를 걸지 않는다. 레거시 `/job/person/list` 도 비로그인에게 목록 전체를 보여준다(이름은 성씨만). 따라서 고칠 것은 **고지 문구**뿐. | 레거시 실측 |
| D | 메일 발송(SMTP)이 아직 없어 알림 계열은 **보류**한다. | [nurse-app-email-sending-limit] |

## 레거시 메타데이터 (원문 그대로)

```
title       널스넷 - 간호사 간호조무사 병원 구인 구직 NO.1
description 상급종합병원부터 동네 병원까지 채용, 연봉, 리뷰, 면접 후기! 간호사·조무사는 '널스넷'에서 확인하세요
keywords    간호사 커뮤니티, 병원구인구직, 간호사구인, 간호조무사모집, 의료취업, 취업,
            간호조무사구인구직, 월급, 연봉, 취업사이트
og:site_name 널스넷 / og:locale ko_KR / theme-color #0044ff / twitter:card summary
```

## 레거시 푸터 회사정보 (원문 그대로)

```
상호 (주)플랫폼몬스터 · 대표 배미미 · 사업자등록번호 363-06-01936
주소 서울시 마포구 월드컵로 81 3층 · 전화 010-5684-6664
이메일 nursenet.co.kr@gmail.com
통신판매신고 제 2022-경기광명-1283호 · 직업정보제공사업 J1513020210002
```

---

# 1. 치명 — 실회원 받기 전 필수

- [x] ~~**1-1. 이력서 "공개" 고지가 실제 공개 범위와 다르다**~~
  화면은 "광고 중인 병원이 볼 수 있습니다"(`mypage/resume/page.tsx:266`)라고 하는데, 실제로는 비로그인 아무나 `/talent`에서 성별·나이·거주지역·학교·경력·희망급여·자기소개를 본다(`talent/page.tsx`에 로그인 검사 없음, `lib/data/talent.ts:179` admin 클라이언트).
  → **오너 확정 C에 따라 게이트는 안 건다. 문구를 사실과 맞춘다.**
- [x] ~~**1-2. `repostJob` 이 결제 대기(draft) 공고를 무료 게시한다 — 결제 우회**~~
  `mypage/actions.ts:287`. 형제 액션 `setJobStatus:768` 은 `.in("status",["open","closed"])` 가드가 있는데 여기만 빠졌다.
- [x] ~~**1-3. `error.tsx` · `global-error.tsx` 가 0개 — 서버 오류 시 영문 흰 화면**~~
  404(`not-found.tsx`)는 한국어로 잘 만들어져 있는데 500만 맨몸이다.
- [~] **1-4. 사업자 인증이 "그 병원의 사업자인지"를 검증하지 않는다**
  `mypage/actions.ts:90-100`. 유효한 사업자번호 하나로 미점유 병원 아무거나 선점 가능. 뺏긴 병원의 이의제기 경로도 없다.
  → 최소 조치: 선점 시 문의 경로 안내. 근본 조치는 별도 논의.
- [ ] **1-5. 알림이 코드에 0건** — 지원 접수·합격·불합격을 아무에게도 안 알린다. (오너 확정 D: SMTP 붙은 뒤)

# 2. 높음

- [x] ~~**2-1. 소셜 가입이 회원유형을 버린다**~~ — 병원을 골라도 간호사 계정이 된다(`LoginButtons.tsx:15`). `next` 도 함께 유실.
- [x] ~~**2-2. 로그인 실패 한 번에 `next` 가 사라진다**~~ — `authErrorPath`(`constants.ts:121`)가 `?error=` 만 만든다.
- [x] ~~**2-3. 홈 지역 검색(`?l=`)이 화면에 안 보이고 해제도 안 된다**~~ — `jobs/page.tsx:168` 판정에서 `loc` 누락. 이후 모든 칩 클릭에 계속 따라붙는다.
- [x] ~~**2-4. 메일 한도 초과를 "이미 가입된 이메일"로 안내**~~ — `(auth)/actions.ts:116` 이 모든 에러를 한 코드로 뭉갠다. 로그도 없다.
- [x] ~~**2-5. 푸터가 홈에만 있다**~~ — 다른 모든 화면에 약관·처리방침·운영주체·연락처가 없다.
- [x] ~~**2-6. 고객센터가 없다**~~ — 결제 오류 문구가 "고객센터로 문의"라고 가리키는데 라우트가 없다(`AdPurchase.tsx:44`). 레거시에는 고객센터 메뉴가 있다.
- [x] ~~**2-7. 탈퇴해도 얼굴 사진이 버킷에 영구히 남는다**~~ — `deleteAccount`(`actions.ts:497`)가 storage 객체를 안 지운다. 처리방침의 "지체 없이 파기"와 어긋난다.
- [x] ~~**2-8. 긴 이력서 폼이 검증에 걸리면 입력 전체가 날아간다**~~ — 검증 4종이 전부 `redirect`.
- [x] ~~**2-9. 돈 낸 광고 공고를 '다시 게시'하면 무료 1건 제한에 막힌다**~~ — `actions.ts:275` 가 이 공고 자신이 유료인지 안 본다.
- [ ] **2-10. 광고 환불·취소 경로가 없고, 지난 영수증 목록도 없다.**

# 3. 중간

## 간호사
- [x] ~~3-1. 기관 종별·직종으로 좁혀 상세에 들어가면 그 조건이 사라진다 (`jobs/[id]/page.tsx:52` 에 `fac`·`cat` 없음)~~
- [x] ~~3-2. 비로그인으로 공고 저장 시 이유 없이 홈으로 버려진다 (`jobs/actions.ts:85`)~~
- [x] ~~3-3. 취소한 지원의 '다시 지원' 이 404 (`mypage/applications/page.tsx:54`)~~
- [ ] 3-4. 워크넷 공고는 로그인해도 외부 링크뿐 — 지원 내역에 안 남는다. 연락처 없으면 없는 곳을 가리킨다(`JobDetail.tsx:216,220`)
- [x] ~~3-5. 탈퇴 비밀번호 확인이 빈칸이면 건너뛴다 (`account/page.tsx:113` 에 `required` 없음)~~
- [ ] 3-6. 이력서를 지우면 게시판·리뷰가 즉시 막히는데 미리 안 알려준다
- [ ] 3-7. 쓴 리뷰를 고칠 수도 지울 수도 없다 (병원당 1건이라 다시 쓸 수도 없다)
- [ ] 3-8. 게시글 수정이 없어 오타 하나에 글을 지워야 하고, 그러면 댓글이 같이 사라진다
- [ ] 3-9. 성별·나이가 인재 카드에 공개되는데 본인이 입력·수정·삭제할 화면이 없다
- [ ] 3-10. 이름이 빈 이력서는 공개해도 검색에 안 뜨는데 화면은 "공개 중"이라 한다
- [x] ~~3-11. 리뷰의 "신고 시 검토 후 비공개 처리" 약속에 대응하는 신고 버튼이 없다~~

## 병원
- [x] ~~3-12. 공고 등록 폼에 마감일 입력이 없다 (`JobFields.tsx`, `createJob`/`updateJob`)~~
- [ ] 3-13. 무료 1건 제한이 제출 후에야 나타나고 입력이 전부 날아간다 (`actions.ts:175`)
- [ ] 3-14. 한 번 연결한 병원을 바꾸거나 끊을 수단이 없다 (`jobs/new/page.tsx:75`, `lib/data/jobs.ts:337` 무정렬 `limit(1)`)
- [x] ~~3-15. 광고 만료 공고가 무료 공고보다 영구히 위에 뜬다 (`lib/data/jobs.ts:192` 정렬이 만료를 안 봄)~~
- [x] ~~3-16. 이메일이 있는 지원자를 '연락처 미입력' 이라 잘못 알린다 (`lib/data/applications.ts:121` CARD_FIELDS 에 email 없음)~~
- [x] ~~3-17. 이력서를 열고 목록으로 돌아오면 필터·검색어·페이지가 사라진다 (`applicants/[id]/print/page.tsx:25`)~~
- [x] ~~3-18. 광고가 살아 있는 공고를 삭제해도 남은 기간·환불 얘기가 없다 (`mypage/jobs/page.tsx:90`)~~
- [x] ~~3-19. 인재 검색에 전화번호 없는 인재의 연락 수단(이메일)이 표시되지 않는다 (`mypage/talent/page.tsx:45`)~~

## 공통
- [x] ~~3-20. `/mypage/*` 12개가 로그인으로 튕길 때 `next` 를 안 넘긴다~~
- [x] ~~3-21. 역할이 반대인 화면은 사유 없이 조용히 `/mypage` 로 튕긴다 (리뷰·게시판의 `CommunityGate` 와 기준이 갈림)~~
- [x] ~~3-22. `profiles` 행이 없으면 헤더는 로그인인데 `/mypage`↔`/login` 무한 고리 (`lib/data/user.ts:62`)~~
- [x] ~~3-23. `/jobs` 목록에 `loading.tsx` 가 없어 전환이 멈춘 것처럼 보인다~~

# 4. 불필요 (지우거나 이름을 고친다)

- [x] ~~4-1. `/about` + `ComingSoon.tsx` — 어디서도 링크되지 않는 빈 껍데기~~
- [x] ~~4-2. `JobNearMeButton` — 화면에서 제일 강조된 버튼인데 안내문 토글일 뿐~~
- [x] ~~4-3. `/mypage/talent` — `/talent` 의 열등한 중복 (필터 3개 vs 6개+패싯)~~
- [x] ~~4-4. 약관 제2·5조 · 처리방침 2항의 "메시지" — DB 테이블은 있으나 UI 0개~~
- [x] ~~4-5. 리뷰의 "신고 시…" 문장 (3-11 과 한 쌍: 버튼을 만들거나 문장을 빼거나)~~
- [x] ~~4-6. `public/next.svg`·`vercel.svg`·`file.svg`·`globe.svg`·`window.svg` — create-next-app 잔재~~
- [x] ~~4-7. '채용 알림' 이름 — 실제로는 저장된 검색 목록~~
- [x] ~~4-8. 홈의 하드코딩 통계("79,000+ / 16,000+")~~

# 5. 사소

- [x] ~~5-1. 공고 삭제·마감이 RLS 에 막혀도 "처리되었습니다" (`actions.ts:768,780`)~~
- [x] ~~5-2. `HospitalPicker` 자동완성에 요청 순번 가드가 없다 (옆의 `HospitalSearchBox` 에는 있다)~~
- [ ] 5-3. 지원 중 세션이 끊기면 쓰던 지원 메시지를 잃는다
- [ ] 5-4. "희망 직종"을 골라도 인쇄 이력서·병원 화면 어디에도 안 나온다
- [ ] 5-5. 이력서를 지워도 사진 카드에는 증명사진이 남는다
- [ ] 5-6. 이미 회원만 들어오는 화면에 "로그인 후 …" 안내가 남아 있다
- [ ] 5-7. 헤더 메뉴가 역할과 무관해 병원이 '리뷰·게시판'에서 벽을 만난다
- [ ] 5-8. 관리자 도구 3종이 링크 없는 '준비 중' 카드로 남아 있다

# 6. 도메인 연결 시 함께 처리

- [ ] 6-1. `NEXT_PUBLIC_SITE_URL` 을 `https://nursenet.co.kr` 로 설정 (지금 바꾸면 레거시가 살아 있는 도메인을 canonical 로 가리키게 된다)
- [ ] 6-2. 포트원 키 투입 → 결제 대기(draft) 공고들이 자연히 풀린다 (오너 확정 A)
- [ ] 6-3. 커스텀 SMTP → 1-5 알림 착수

# 7. 오너 추가 요청 (2026-07-30)

- [x] ~~**7-1. 이력서·공고 작성 중 임시저장**~~ — "쓰다 중단하면 처음부터 다시 해야 해서 스트레스"(오너).
  `src/components/FormDraft.tsx` 신설. 이 브라우저(localStorage)에 자동 보관하고 다음에 열면 이어 쓴다.
  · 적용: 이력서(`/mypage/resume`), 공고 등록(`/mypage/jobs/new`), 공고 수정(`/mypage/jobs/[id]/edit`)
  · 경력처럼 **줄이 늘어나는 폼**은 복원할 때 줄 수부터 맞춘다(+ 경력 추가 버튼을 필요한 만큼 누른다)
  · 제출했다고 바로 지우지 않는다 — 서버 검증에 걸려 되돌아오는 경우가 이 기능이 가장 필요한 순간이다
  · 개인정보: 키에 계정을 넣어 같은 PC 의 다른 사람이 못 읽게 하고, 7일 지나면 버리고, 화면에 **지우기 버튼**을 항상 둔다
  · 한계: **이 브라우저에만** 남는다(다른 기기에서는 안 보인다). 서버 보관은 DB 작업이 필요해 이번엔 안 한다.

# 8. 1차 수정이 만든 회귀 — 커밋 전 검증(4인)에서 잡아 고침

- [x] ~~**8-1. 네이버 회원이 탈퇴를 못 하게 됐다**~~ — 탈퇴 비밀번호 확인을 넣으면서 identity 이름으로
  판정했는데, 네이버 계정은 우리가 `admin.createUser({email})` 로 만들어 비밀번호가 없는데도
  provider "email" identity 가 붙는다 → 있지도 않은 비밀번호를 요구해 영구히 막혔다.
  소셜 여부(`metadata.provider === "naver"` 또는 non-email identity)로 판정하도록 고침.
- [x] ~~**8-2. 마감일을 넣었더니 병원 화면이 404 로 안내했다**~~ — 구직자 쪽(isOpenToSeekers)은 마감일을 보는데
  병원 쪽(jobState)은 몰라서 "무료 ···까지 (N일 남음)" 이라 말하고 제목을 누르면 404. `jobState` 가
  `deadline` 을 **필수 인자로** 받게 고쳐(빠뜨리면 컴파일이 잡는다) 두 화면의 판정을 하나로 맞춤 + 테스트 추가.
- [x] ~~**8-3. 정작 동의를 받는 체크박스만 옛 문구였다**~~ — 공개 범위 문구를 세 곳 고치면서 **첫 저장 시
  동의 체크박스**를 빠뜨렸다. `RESUME_PUBLIC_SCOPE` 상수 하나로 묶어 네 곳이 다시 어긋나지 못하게 함.
- [x] ~~8-4. 네이버 role/next 쿠키가 다음 로그인에 새어들던 것~~ — 시작 라우트에서 항상 set 또는 delete.
- [x] ~~8-5. 탈퇴 실패 시 증명사진만 먼저 지워지던 것~~ — 파일 삭제를 계정 삭제 성공 뒤로 옮김.
- [x] ~~8-6. 과거 날짜 마감일이 그대로 저장되던 것~~ — 서버에서 되돌려 알리고(`?error=deadline`) 입력에 `min`.
- [x] ~~8-7. 공고 상태 조회 실패 시 지원 내역 전부가 '마감'으로 보이던 것~~ — 열린 쪽으로 기울임(fail-open).
- [x] ~~8-8. repostJob 이 방금 승인된 결제의 광고를 지울 수 있던 것~~ — 만료 상태인 행에만 표식을 지우도록 좁힘.
- [x] ~~8-9. 인쇄 이력서 PDF 하단에 푸터가 딸려 나오던 것~~ — `SiteFooter` 에 `print:hidden`.

# 9. 2차 검증(2인)에서 또 잡힌 것 — 1차 수정의 미완·부작용

- [x] ~~**9-1. 마감일 반영이 절반만 됐다**~~ — `jobState` 는 고쳤는데 `listingEnd` 는 그대로라,
  "무료 8월 6일까지 (7일 남음)" 이라 적어놓고 실제로는 내일 사라지는 공고가 생겼다. 대시보드
  '마감 임박(3일)' 위젯도 그 값을 써서 존재 이유가 무력화됐다. `listingEnd` 도 `deadline` 을
  **필수 인자**로 받아 둘 중 먼저 오는 쪽을 돌려주게 고침 + 테스트 5건 추가.
- [x] ~~**9-2. 탈퇴 면제 판정을 위조 가능한 값에 걸어뒀다**~~ — `user_metadata` 는 세션 소유자가
  브라우저에서 직접 쓸 수 있어 서버 판정 근거가 못 된다. 네이버 표식을 `app_metadata`(service_role
  전용)로 옮기고, 판정을 "email identity 가 있으면 요구, 단 네이버 계정은 예외"로 바꿔
  이메일+카카오 연결 계정도 비밀번호를 요구하게 함. 비밀번호를 넣어서 오면 **항상** 검증한다.
- [x] ~~**9-3. 임시저장 초안이 로그아웃·탈퇴 후에도 최대 7일 남았다**~~ — 초안에는 실명·휴대폰·
  근무 병원명이 평문으로 들어간다. `DraftCleaner` 를 만들어 로그아웃·탈퇴 도착 화면(`/?left=1`)에서
  전부 지우고, 저장 성공(`?ok=`) 시에도 즉시 버리게 함.
- [x] ~~9-4. 다른 폼의 오류로 초안이 되살아나던 것~~ — `ownErrors` 로 이 폼의 오류 코드일 때만 복원.
- [x] ~~9-5. 무료 1건 제한이 다른 화면으로 보내 입력·초안이 함께 날아가던 것~~ — 같은 폼으로 되돌린다.
- [x] ~~9-6. 초안에서 지운 경력 줄이 되살아나던 것~~ — 초과분을 비워 서버 필터가 버리게 함.
- [~] 9-7. 공고 등록 초안에 **병원 선택은 담기지 않는다**(hidden 값) — 복원 배너에 "병원은 다시
  선택해 주세요" 를 띄우는 것으로 갈음. 근본 해결(선택값도 초안에 담기)은 남겨둠.
- [ ] 9-8. `RESUME_PUBLIC_SCOPE` 문구가 아직 두 군데 부정확 — (a) `resume_title`(이력서 제목)이
  비로그인에게 공개되는데 목록에 없다, (b) 이름·연락처는 '광고 중인 병원'뿐 아니라 **내가 지원한
  병원**도 본다. 다음 차례에 상수 한 곳만 고치면 세 화면이 함께 맞는다.

# 11. 10장 검증(2인)에서 잡힌 것 — 같은 커밋에 반영

- [x] ~~**11-1. 레거시 게시글 2,245건은 실제로 이어줄 수 있었다**~~ — 내 주석이 "대응할 페이지가 없다"고
  단정했는데 틀렸다. 게시글 2,856건은 이관됐고 `board_posts.legacy_srl` 에 라이믹스 `document_srl`
  이 그대로 있다. `/community_board/{srl}` 을 **실제 글로** 보내는 라우트 핸들러를 만들었다
  (config redirects 는 DB 를 못 보므로 이 경로만 분리). 옛 북마크·검색 유입이 빈 목록 대신 그 글로 간다.
- [x] ~~**11-2. reviews 에 컬럼 단위 쓰기 잠금이 없었다**~~ — authenticated 가 테이블 전체 UPDATE 를
  갖고 있어, 본인 JWT + 공개 anon 키로 PostgREST 를 직접 부르면 자기 리뷰의 `hospital_id` 를 바꿔
  **다른 병원 평점으로 옮기거나**, 신고로 숨겨진 리뷰(`is_hidden`)를 되살리거나 `report_count` 를
  0 으로 되돌릴 수 있었다. 게시판과 같은 형태로 좁혔다(마이그레이션 `20260730120000`, DB 적용 완료:
  이제 `rating·content·work_period` 만, anon 은 전부 회수).
- [x] ~~11-3. 이력서 삭제 확인창이 사실보다 축소돼 있던 것~~ — 막히는 건 쓰기만이 아니라 **읽기·수정·
  삭제 전부**다. "지우려면 이력서 삭제 전에 먼저 정리하세요"까지 적었다.
- [x] ~~11-4. 수정 흔적이 안 남던 것~~ — 댓글이 달린 글의 본문을 통째로 갈아치워도 알 수 없었다.
  `updated_at > created_at` 이면 "수정됨 (날짜)"를 붙인다(트리거가 이미 유지 중이라 조회만 추가).
- [x] ~~11-5. 병원별 리뷰 화면에는 수정·삭제 버튼이 없던 것~~ — 병원당 1건이라 그 화면이 오히려
  자연스러운 진입 경로다. 두 화면이 같은 `OwnerActions` 를 쓰게 했다.
- [x] ~~11-6. /notice·/community·/schedule 을 308 로 선점하던 것~~ — 흔한 이름이라 나중에 같은 이름
  페이지를 만들면 영원히 안 열린다(redirects 가 파일시스템보다 먼저 돈다). 307 로 낮췄다.
- [x] ~~11-7. /board_qna/:id 규칙 누락~~ — 추가.
- [x] ~~11-8. 공개범위 문구 하드코딩 2곳~~ — 사진 안내·공개 완료 배너도 상수를 쓰게 했다(이제 5곳 전부).
